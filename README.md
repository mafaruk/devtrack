# devtrack
a mini task/project manager (like a tiny Jira) that naturally forces you to use every piece you listed: React frontend, a Java Spring Boot core API (Security + Transactions + AOP), a Python FastAPI microservice for notifications/analytics, Kafka connecting the two, then Docker → Kubernetes → AWS EKS for deployment.
